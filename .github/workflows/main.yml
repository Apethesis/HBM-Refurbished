on:
  push:
    branches:
    - main
    - unstable
jobs:
  buildhbm:
      name: Builds HBM Refurbished Edition
      runs-on: ubuntu-22.04
      steps:
        - uses: actions/checkout@v4
        - uses: actions/setup-java@v4
          with:
            distribution: 'temurin'
            java-version: '8'
        - name: Build with Gradle
          run: ./gradlew build

        - name: Upload built jar
          uses: actions/upload-artifact@v4
          with:
            name: Unstable
            path: build/libs
      
